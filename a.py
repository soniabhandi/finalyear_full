import json 
dictionary ={ 
    "e" : 12, 
} 
  
value = input('e: ')
for i in di:
    user['username'] = username
    user['phone'] = phone

with open("task.json", "w") as outfile: 
    json.dump(dictionary, outfile)